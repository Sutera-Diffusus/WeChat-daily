# Changelog

## Unreleased

- 简报体裁分层：所有产出通道统一同一把成稿标尺——≥2 条同主线证据、或单条本身构成完整公告/决定/请求/期限、或模型对单句高信息证据写出了 ≥120 字真实编辑稿——满足才成卡片；本地重建与补回的内容一律降级为“线索速览”（发言人+原句+保留原因），不再生成模板正文与分类式假标题；
- 模型卡片标题失效时改为从导语首句提炼具体判断式标题，不再落回“主题分类：固定后缀”模板；本地导语/态势的事件标题去重；
- AI 文体规则升级：正文新闻点先行、引语融句、禁止罗列体与媒体套话（引发关注/持续升温/拭目以待/业内人士表示等），标题必须是具体判断而非分类标签；
- DeepSeek 推理模型健壮性：空正文自动从推理通道回收 JSON，并内置一次热缓存重试应对推理耗尽输出预算（finish=length）的瞬时抽签失败；- 简报产出升级为正式产品标准：AI 分析载荷新增 `masthead`（产品、窗口、主体、统计、来源、生成时间）与 `user_identity`，简报头部展示“本期为谁整理”；
- 每条 finding 增加确定性署名层：`participants` / `speakers` 由本地根据引用证据生成（谁提出、谁回应、具体表述摘录），模型返回的 speakers 仅在与证据一致时保留；前端日报卡片与 AI 面板渲染“发言人”区块；
- 修复用户主体识别：适配器暴露登录账号的真实显示名（`self_display_name`），画像新增 `self_name` 设置项，AI 上下文与 prompt 明确“我”是谁；`/api/ai-analysis` 的本地基线补上此前漏传的 profile；`self_name` 参与“与我相关”匹配；
- 修复私聊被统一标低重要性：prompt 增加私聊重要性校准基准（私聊请求/安排/承诺/决定/风险不低于 60），本地编辑分对全私聊证据的 finding 加成并设置 55 分可见下限，`rule_level` 明确只是保守初筛下限。
- 修复 DeepSeek/OpenAI 兼容路径提示词缓存命中率极低：整段固定编辑规则移入 system 消息形成逐字节稳定前缀，用户消息只保留易变的窗口、主体、候选与聚合上下文；新增 `prompt_cache_hit_tokens`/`cached_tokens` 用量日志便于核验缓存效果。
- 修复 DeepSeek 推理模型（deepseek-v4-flash 等）的两种空正文故障：显式设置 `max_tokens=32768` 防止推理耗尽默认输出预算；当 `finish_reason=stop` 但 content 为空时，从 `reasoning_content` 中回收完整 JSON（实测模型把整份答案写进推理通道）。
- 修复“与我相关”车道两个缺陷：`self_name` 按字符串整体匹配（此前被误拆为单字符导致凡含该字符的消息全部误判 for_me）；私聊中携带明确请求标记与具体对象的单条消息（如“把选题报给教务系统，别忘了”）不再需要多条成簇才能进入 for_me 车道。

## v0.1.4 (2026-08-24)

- 建立微语 0.1.4 的首个可复现源码快照；
- 整理本地微信接收、历史档案、可解释分析和只读控制台的公开说明；
- 保留 `wechatauto_db`、`wxauto4` 和 Hook HTTP 三类适配边界；
- 完成 Tauri 2 桌面端、PyInstaller sidecar 和 Codex 本地插件的仓库结构说明；
- 将数据库、浏览器 profile、运行日志、临时截图、安装包和构建目录排除在版本库之外；
- 补充规则配置示例、贡献指南、安全策略和 MIT License。
